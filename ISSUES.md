# Issues — Canvassing Planner

## Open
- [ ] **Route size tails on isolated estates** — most routes land within ~15% of target after diffusion rebalancing, but pockets bounded by a river/rail/bypass with no shared street node to a neighbour can't be reached by the balancer and stay small or get folded into an oversized neighbour instead *(found 2026-07-08)*
## Resolved
- [x] **Sequential route numbers + colour realignment on every render** — route display numbers ("Route N" in the list, tooltips, doc pages, print sheets) are now the route's position in the list, reassigned every render, so deletes/splits never leave gaps; `rt.id` remains the stable internal key (marks, orphans, splits). Colours are likewise reassigned from the 8-colour palette by list position each render, retiring any brown/grey saved in older plans *(resolved 2026-07-18)*
- [x] **Brown and grey dropped from the route palette** — both the backend and the client split-route palette now cycle 8 colours (Tableau-10 minus #9c755f brown and #bab0ac grey, which vanish against the basemap) *(resolved 2026-07-18)*
- [x] **Orientation POIs on route maps** — /wardmap now also fetches stations/halts, bus stations, and city/town/village/suburb/neighbourhood centres in a ~3 km fringe around the ward (one Overpass call, cached). Each route map marks the nearest station 🚉, bus station 🚏, town/city centre and local centre in blue: a dot + name if on-map, otherwise an arrow at the map border pointing toward it with the distance. The 4 most prominent main roads (ward-wide length × road-class weight, motorway→tertiary — footpaths excluded) are marked where they head off the map, or with a direction arrow + distance if out of view. Markers share the street-label collision system so nothing overlaps. Verified backend on the real area: 22 POIs incl. Pontefract Tanshelf station + bus station *(resolved 2026-07-18)*
- [x] **Sparse streets first + "Take from route" area tool** — Sparse streets moved to the front of the route-page toolbar. New "⊕ Take from route" area marker (green): drag a region and every segment belonging to *another* route (or sitting in the orphan pool) inside it is queued as a pending take, drawn green-dashed; Apply marks commits — segments leave their source routes (which replan; a source emptied entirely dissolves), join this route, and this route replans, all in the same apply as any removal/orphan marks. Clear marks discards pending takes; Reset route returns previously-taken segments to the orphan pool (their sources have already replanned without them) *(resolved 2026-07-18)*
- [x] **Route can dissolve when fully marked** — Apply marks with every segment marked (any mix of "not to visit" / "for other route") now offers to delete the route outright (confirm dialog): removed segments drop, orphaned ones join the orphan pool for reattachment, and the route leaves the list/map/saved state. Previously a fully-marked route just reset itself *(resolved 2026-07-18)*
- [x] **6 m walkability fringe on the ward boundary** — ward boundaries often run down the middle of a road/footpath, leaving that way outside the fetched street network. `get_graph` now buffers the polygon ~6 m (true metres, lat-corrected) before fetching, so boundary roads/paths join the walk network for every endpoint that routes (/plan, /snap, /reroute, /wardmap); `/addresses` still uses the unbuffered polygon, so no out-of-ward houses are added. Verified: 14 graph nodes appear just outside a test boundary *(resolved 2026-07-18)*
- [x] **Suggest non-residential/open land broken** — the new amenity=parking tag pulls OSM node (Point) features, so the open-land blob's union became a GeometryCollection with Points that turf.union can't consume, killing the Suggest click silently. `fetch_landuse_blobs` now filters to Polygon/MultiPolygon before unioning. Requires a "Reload addresses" to refresh the cached geoms *(resolved 2026-07-18)*
- [x] **Cutout suggester missed parks/car parks/graveyards; sparse streets now colour-coded** — `fetch_landuse_blobs`'s open-land fetch gained leisure (park, recreation ground, garden, pitch, golf course, playground, sports centre), amenity (parking, grave_yard) and landuse (cemetery, recreation_ground, village_green) tags — on the real ward this adds 2.81 km² of parks, 0.52 km² of car parks and 0.22 km² of cemeteries to the suggestion. The 💡 Sparse streets panel now also recolours the route on the map by density — red for very sparse (≥60 m/addr or no addresses) through to green for dense (≤8 m/addr), log-interpolated — with the same colour on each row's ratio; closing the panel restores the route's own colour. Verified: all 254 segments recoloured spanning hue 0→120, restored on close *(resolved 2026-07-17)*
- [x] **Two mark types + orphan reattachment** — the click-to-mark "Mark streets" mode is gone (area marking won); in its place two area tools: "▨ Mark: not to visit" (grey-dashed, dropped entirely on apply) and "⇄ Mark: for other route" (purple-dashed, on apply the segments leave the route and join a persistent orphans list, shown on the main map as purple dashed lines with a tooltip naming the orphaning route); "Apply marks" applies both kinds in one recalculation, Sparse-street rows mark as not-to-visit, and Reset route also reclaims its own orphans. The Routes step gains "🧷 Attach orphaned streets (N)": each orphan joins the geographically nearest route that ISN'T the one that orphaned it, every gaining route replans, and the map/list re-render. Orphans persist in saved state. Verified: orphaned 3 segments from route 1 (254→251), purple overlay + count button appeared, attach moved them to a neighbouring route and replanned it, zero JS errors *(resolved 2026-07-17)*
- [x] **"Sparse streets" suggester on route pages** — 💡 Sparse streets toggles a panel listing every street in the route with its total length, address count, and length-per-address ratio, sorted sparsest first (zero-address streets show ∞), so a removal threshold can be judged by eye; clicking a row marks/unmarks that whole street through the existing mark → Apply removals flow, and the panel recomputes after removals apply. Verified: 27 streets listed for the test route (7.6 km of footway at ∞ m/addr top, 1 m/addr residential bottom), row click marked 176 segments on the map and enabled Apply *(resolved 2026-07-17)*
- [x] **Polish round: Clear marks, instructions placement, preview zoom, one Back** — "Unmark area" replaced by "🧹 Clear marks" (wipes all street/area marks in one click); the volunteer "Route instructions" now render at the top of every route document's Notes page, and their edit box moved to the top of the Routes sidebar step; on-screen document pages render at 1.5× zoom (print resets to true A4); the redundant "← Back to all routes" button removed — the sidebar Back is the app's single back control (from a route page it closes the page first, staying on Routes, then walks the steps) *(resolved 2026-07-17)*
- [x] **Route map letterboxed inside its box** — the SVG's geographic aspect ratio rarely matched the box the A4-fitting logic allotted, so preserveAspectRatio centred it with blank bands. Now the text content lays out first, its leftover page-1 space fixes the map box, and the box's aspect ratio bounds the map geography (one axis grows, centred, spending the spare direction on extra surrounding context) — verified box aspect 1.271 vs SVG 1.269, rendered height exactly the box height *(resolved 2026-07-17)*
- [x] **Label placement relaxed + Split Route tool** — short-street labels are no longer forced outside the route area: each tries a ring of nearby spots, rejecting any that overlap an already-placed label and preferring ones clear of roads (coarse road-cell grid test); the outside-with-leader-line placement is now only the fallback for genuinely crowded spots. New "✂️ Split route" on the route page: press-drag a chord across the route, the two connected sides are computed client-side (union-find over the surviving edges; crossed edges and stray pieces attach to whichever side they touch), "verify" recolours side B blue with Confirm/Cancel, and Confirm turns the route into two — both get their street tables/main-road names (named streets preferred) rebuilt and their walk orders replanned via /reroute, the route list re-renders with the new route inserted, and everything saves. Verified with a real mouse-drawn chord: 842-segment route split 451/391 segments (317+102 addresses), 23→24 routes, no JS errors *(resolved 2026-07-17)*
- [x] **Doc-prep round: precompute + progress bar, nav at top, mark-then-apply removals, richer maps** — route-map data is now computed at the end of the planning step, not lazily per page: the /plan stream emits enumerable progress ("Computing walk orders… 32/59" renders as a real bar), then one ward-wide /wardmap fetch (all buildings + roads in a single Overpass call — replaces the old per-route /routemap) and a per-route footprint/hover-zone pass with its own bar; route pages then open instantly. Back/Next moved to the top of the sidebar above the tools. Street removal on a route page is now mark-then-apply: click segments (marked ones go grey-dashed), or freehand "Mark area"/"Unmark area" region tools (same press-drag gesture as the cutout step, hit-testing via turf.booleanIntersects through the SVG projection), then one "Apply removals" does a single /reroute recalculation; Reset route still restores the pre-edit original. The vector map got richer: lat/lon coordinate grid with degree labels on all four edges, route drawn at 50% opacity, the area outside the route's margin shows the regular map at 80% opacity (dashed demarcation line removed — the detail contrast marks the boundary), margin widened 50→75 m, street labels enlarged with white halos, and the route's short streets get labels pushed outside the route area with leader lines. Verified end-to-end in the browser incl. mark/apply/reset on a 842-segment route *(resolved 2026-07-17)*
- [x] **"Stuck on planning"** — both /plan requests had actually completed (200 OK); the hang was perceptual: a snapped MultiPolygon fetches each piece from Overpass silently, and the elevation service stalled up to 90 s per batch. Added a "Shape has N separate pieces…" progress message and cut the elevation batch timeout to 30 s *(resolved 2026-07-17)*
- [x] **Major restructure: step-based wizard + document route pages** — sidebar is now a 6-step flow (Choose ward → Addresses → Area cutout → Snap to roads → Plan → Routes) with Next/Back; Back lets earlier steps be viewed, and using an editing tool with a plan in hand asks "Editing this will clear all data for steps after — proceed?" before dropping later-step data; full state auto-saves after every step/mutation and a refresh resumes exactly where you were. Step 6 is a slim list of route hyperlinks (no street tables in the sidebar); hovering a route's line *or its whole area* (invisible buffered-footprint hover zones) highlights it. Clicking a link opens a Route Page: an A4-styled document over the map area with a **vector SVG map at the top** (OSM buildings, roads, street-name labels, route in colour, start triangle — new /routemap endpoint, cached), the street/address table, walk order, and a ruled Notes page; print CSS renders the pages as true A4 with a fit-to-page map height. Route Pages support **click-to-erase**: toggling Erase path and clicking a segment removes it, recomputes the street table/addresses client-side (edge features now carry u/v/k + per-edge address counts) and replans the walk order via the new stateless /reroute endpoint (build_walk extracted from _steps, covered by a new test); first edit snapshots the original so Reset route restores it. Verified end-to-end in the browser: full 6-step walk-through, plan → 59 links, doc page with 115 buildings/98 roads/44 segments, erase 167→154 addresses live with the toggle staying on, reset, back, rewind-confirm cancel/accept, refresh-resume *(resolved 2026-07-17)*
- [x] **UI round: Reset, no Restore button, one-command Snap & tidy** — "Restore last plan" removed (a refresh now silently auto-restores; the floating top-right "🧹 Reset" wipes localStorage AND reloads, so Reset genuinely resets); "Full ward" button removed; "Restart cutout" moved to the bottom of the cutout section; applied working shape restyled to bold orange lines (#f28e2b, weight 4, faint fill); snap/expand/straighten collapsed into one "🧲 Snap & tidy shape" command with the expand margin raised to ~5 m; "Edit shape vertices" renamed "✥ Tweak" *(resolved 2026-07-17)*
- [x] **Snap & tidy refused MultiPolygon shapes** — found via the user's real session: a cutout that splits the ward produces a MultiPolygon, which the snap step rejected as "nothing to snap" (frontend required type Polygon; /snap only handled Polygon/LineString). Backend now snaps each MultiPolygon piece and unions; frontend accepts both. Verified end-to-end on a deliberately split ward (74 → 55 points, stays MultiPolygon) *(resolved 2026-07-17)*
- [x] **Wizard round: Clear button, dividers removed, snap → expand → straighten tidy pipeline** — added "🧹 Clear & start again" at the top (clean reload; the saved plan survives so Restore undoes an accidental Clear); removed the Draw/Undo divider buttons and all their plumbing (backend `dividers` param kept, defaults to empty); replaced "Snap to roads & adjust" with three separate assessable steps: 1) snap shape to roads (unchanged /snap), 2) expand ~1.5 m outward (turf.buffer margin so straightening never clips boundary houses), 3) straighten edges (turf.simplify Douglas-Peucker — keeps the outermost point of each curve, drops the small in-between wiggles). Verified on the real ward polygon: 68 → 47 (snap) → 154 (expand adds corner arcs) → 44 points (straighten), leaving vertex editing manageable *(resolved 2026-07-16)*
- [x] **Route document window overlapped neighbouring routes; PART streets unidentifiable on it** — the single-route view's mask was a convex hull (spilled over adjacent routes' streets); now it's the route's own streets buffered ~50 m (turf.buffer + union, hull as fallback), plus a faint dashed trace of the route's edges so a volunteer can see exactly which section of a "PART" street is theirs *(resolved 2026-07-16)*
- [x] **Plan and notes lost on refresh** — the last plan (routes + per-route notes) now persists to localStorage (saved after render and on notes change; ~1.5 MB for a 59-route ward); a "Restore last plan" button appears on load when one exists, verified surviving a real refresh *(resolved 2026-07-16)*
- [x] **Restore brought back routes but none of the wizard steps** — the ward context (ward boundary, eroded working shape, dividers, ward label, shapeModified) is now saved alongside the plan and rebuilt on restore, so shape editing / snap / cutouts / dividers / re-planning all work after a refresh, not just a read-only route list. Addresses are automatically re-fetched after restore (server-side, fast), re-enabling the Suggest button and the live route estimate without any manual step *(resolved 2026-07-16)*
- [x] **No walking distance/time per route** — each route now reports its full Chinese-postman circuit length (incl. deadhead repeats, from the graph's own edge lengths) as "X km walk · ≈NhMM" (4 km/h + 15 s per letterbox) on the card, route page, and printed sheet *(resolved 2026-07-16)*
- [x] **Print pack was all-or-nothing** — the route page gained a "Print this route" button producing that route's single sheet (shared sheetHTML with the full pack) *(resolved 2026-07-16)*
- [x] **Route size spread widened (41-271 vs target 175) — root-caused and fixed** — instrumented the real whole-ward inputs: the old largest-route-first rebalancer livelocked (577 of 664 moves shuffled zero-address edges that change nothing but reset the stuck-tracking, 6919 iterations rejected on connectivity, exit via the 8000-iteration cap). Rewrote `_rebalance` as pairwise diffusion with three street-network-specific mechanics: adjacent pairs balance directly (an undersized route is fed by its own neighbours even while a bigger imbalance elsewhere is stuck), moves transfer boundary *bundles* (a boundary edge plus its dangling cul-de-sac branch — single-edge moves always failed the donor-connectivity check in tree-like areas), and all-zero-address boundaries (footpath belts) advance the small route's frontier one zero bundle so addressed streets become reachable next pass. Whole-ward smoke test: 147-204 addresses across 59 routes (tighter than the pre-regression 174-222), full coverage preserved, rebalance itself <1s; covered by a new zero-belt test *(resolved 2026-07-16)*
- [x] **Stale index.html served from browser cache ("terrain buttons still around")** — the file itself was already clean; `FileResponse` sent no Cache-Control so the browser heuristically cached the page and served it without any network request. Added `Cache-Control: no-store` to `/`, one hard refresh clears the stale copy permanently *(resolved 2026-07-11)*
- [x] **Streets with no addresses of their own could vanish from every route — confirmed real bug, root-caused and fixed** — `partition()` only ever seeds a new chunk from an address-bearing edge; a through-road with no direct frontage (exactly what Halfpenny Lane/Western Avenue sound like) positioned beyond an already-quota-satisfied route had no seed of its own and nothing else reached it, so it was silently dropped entirely. Reproduced in isolation (5 of 6 edges vanished), fixed with a sweep pass that pulls leftover edges into whichever chunk touches them (or makes them their own chunk if a whole component seeded nothing), covered by a new test, and confirmed both streets now appear when planning the real ward *(resolved 2026-07-11)*
- [x] **Terrain removed** — wasn't earning its keep; `/terrain` endpoint and all its frontend (Show/Hide button, opacity slider, `terrainLayer`) deleted. Manual divider-drawing kept as a general-purpose "split routes here" tool, relabelled since it no longer depends on a gradient view *(resolved 2026-07-11)*
- [x] **Cutout suggestion didn't account for large fields/open land** — `fetch_landuse_blobs()` gained a third fetch (farmland/meadow/orchard/forest/grass + wood/grassland/scrub), unioned into the same "Suggest" draft as commercial/industrial land *(resolved 2026-07-11)*
- [x] **Route names were geocoded neighbourhoods (slow, external dependency) instead of the route's own main road** — deleted the async Nominatim reverse-geocode loop entirely; `rt.name` is now just `rt.main`, set synchronously — routes render instantly instead of trickling in over ~1.1s each *(resolved 2026-07-11)*
- [x] **Final view too busy; no way to tell routes apart at a glance** — address dots now hidden once routes are drawn (`render()` removes `addrLayer`, restored on next ward selection); hovering a route (its line or its sidebar card) now highlights it and dims every other route to grey *(resolved 2026-07-11)*
- [x] **No per-route "document" view** — added an in-app (not a real separate page) single-route view: map fits to the route's bounds with a `turf.mask()` blanking everywhere else, a plain basemap with just the downhill start-triangle (no colour overlay — clean enough to screenshot), and a sidebar panel with the street/address table, walk order, and a new Notes textarea that persists per-route and now also appears on the printed sheets *(resolved 2026-07-11)*
- [x] **MultiPolygon cutouts only ever planned one shape — confirmed root cause via direct osmnx test** — `ox.graph_from_polygon()` silently drops all but one piece of a MultiPolygon (built a 2-piece MultiPolygon from real, independently-verified-to-have-streets areas; the combined fetch returned nodes 100% from piece one, zero from piece two). `get_graph()` now fetches each `poly.geoms` piece separately and `nx.compose_all()`s them. Confirmed fixed end-to-end: the same test polygon now plans 4 routes instead of 3, the ">1 disconnected areas" log message fires, and one route (101 addrs, "Farthing Avenue") is clearly the previously-lost second piece *(resolved 2026-07-11)*
- [x] **Walk-order line could draw the wrong parallel street — confirmed via direct networkx test** — the per-route walk graph was built as a plain `nx.Graph()`, which silently collapses two different real streets sharing both endpoints into one edge; reconstructing geometry then picked an arbitrary parallel edge (`d[min(d)]`), not necessarily the one actually in that route. Fixed by building it as `nx.MultiGraph()` (verified `nx.eulerize`/`eulerian_circuit(keys=True)` both handle MultiGraph correctly) plus a fallback map for eulerize's synthetic duplicate-edge keys (confirmed these get a key not present in the original graph) *(resolved 2026-07-11)*
- [x] **Routes sprawled into neighbouring areas instead of staying compact** — `partition()`'s BFS frontier grew in pure graph-hop order with no relationship to geographic compactness; replaced with a `heapq`-based nearest-to-seed growth (flood-fill by distance from the seed edge, not discovery order), covered by a new test proving a geographically-near edge gets picked before a graph-adjacent-but-far one *(resolved 2026-07-11)*
- [x] **Terrain was an unrelated area grid; "relative path heights" wanted instead; no real toggle** — replaced `/elevation_grid` (independent lat/lon grid sampling) with `/terrain` (per-street-edge slope, reusing the same `get_graph`/`fetch_elevation` caches `/plan` already relies on for hill-barriers/the downhill marker); frontend now colours the actual streets instead of a grid, and Show/Hide is a real toggle that never re-fetches *(resolved 2026-07-11)*
- [x] **Cutout mask could only be added to; no fine-adjustment tool; "Undo last applied cutout" redundant** — added a symmetric erase brush (same hand-rolled press-drag-release mechanism, `turf.difference` into the draft instead of `turf.union`) so mistakes can be corrected precisely before Apply; removed "Undo last applied cutout" and the `cutoutUndo` stack per the user's own reasoning that two-way draft editing makes it redundant, replaced with a simple `shapeModified` flag for the one thing that still needed it (the `/snap` "has this shape been touched" check) *(resolved 2026-07-11)*
- [x] **Suggested cutout showed a wall of draggable vertices; freehand drawing didn't work at all** — two bugs: (1) `showDraftCutout()` called `.pm.enable()` on the suggested shape, rendering a Geoman vertex handle at every point of the real OSM landuse polygon (often hundreds); removed — the draft cutout is now a pure "erase mask" visual with no vertex editing, only extended by drawing more strokes over it. (2) Geoman's `enableDraw('Freehand', ...)` is a PRO-only feature and threw `Cannot read properties of undefined (reading 'enable')` in the free build we use — replaced with a hand-rolled press-drag-release freehand tool using plain Leaflet mouse events (`map.dragging.disable()` for the stroke's duration, points collected via `mousemove`, finished polygon unioned into the draft on `mouseup`); also a real, separate bug: `workingLayer`/`wardLayer`/terrain cells/divider lines were all mouse-interactive by default, so a filled overlay covering most of the map silently ate the mousedown a freehand stroke needs — set `interactive:false` on all of them. Verified with genuine Playwright mouse drags (not synthetic events), including drawing directly on top of an existing hatch-shaded draft *(resolved 2026-07-11)*
- [x] **Area cutout conflated with address-level exclusion, vertex-only drawing, no visual distinction for removed land, one global route-count target across disconnected pieces** — split "Area cutout" (land-shaping: freehand-drawn, hatch-shaded black, editable before applying, with an OSM-landuse "Suggest" starting point) from "remove commercial/industrial addresses" (a pure count filter, always on, never touches geometry — this was already how the backend worked, just mislabelled in the UI); manual cutout drawing now uses Leaflet-Geoman's native Freehand mode instead of click-vertex, with `snappable:false` so it doesn't jump to address dots; a black diagonal hatch (`leaflet.pattern`'s `L.StripePattern`) marks land about to be removed while it's being drawn/adjusted; Snap to roads + a new "Edit shape vertices" toggle now sit right after the cutout step; backend gained `partition_by_component()` so a cutout that splits the street network into disconnected pieces gives each piece its own target-based route count instead of one calculation pooled across areas that were never routable together *(resolved 2026-07-11)*
- [x] **Wizard polish: legacy checkboxes, input styling, live route estimate, cutout snapped to addresses** — removed the old blanket "exclude industrial" / "split at hills" checkboxes (superseded by the Erase and Terrain wizard steps; /plan just uses its pydantic defaults now); restyled the ward search box and target input to match the button look; moved "target addresses per route" to after Load addresses/Erase with a live "N addresses -> X-Y routes" estimate (±20% band, recomputed via `turf.booleanPointInPolygon` against the current eroded shape on every erase/undo/snap); the whole ward now shades immediately on selection so cutouts read as an eraser punching holes in it; fixed Geoman's default vertex-snapping magnetizing the cutout/divider pens onto address dots (`snappable:false`) *(resolved 2026-07-11)*
- [x] **No guided ward-wide workflow** — added a step-by-step wizard once a ward is selected: load & colour-code every address (red/gold/grey = residential/commercial/industrial), erase commercial/industrial land or a hand-drawn cutout (turf.js, subtracted from the working shape), a local-slope terrain overlay (not raw altitude) with an opacity slider for drawing hill dividers, a "snap to roads" clean-up pass, and a downhill "top of route" marker (elevation-based, walk order starts there via `nx.eulerian_circuit(..., source=)`) *(resolved 2026-07-09)*
- [x] **turf.js v7 API break (union/difference)** — code was written against the v6 two-argument signature; v7 requires a single `featureCollection`. Erase tools (auto and manual cutout) threw `Uncaught Error: Must have at least 2 geometries` on first use *(resolved 2026-07-09)*
- [x] **/addresses returned commercial/industrial land in the wrong CRS** — `fetch_landuse_blobs()` works in EPSG:3857 for accurate metre-based `.within()` checks, but the geometry was sent to the browser (WGS84) without reprojecting back; "Remove all commercial/industrial" silently no-opped (turf.difference against a shape in the wrong coordinate space) *(resolved 2026-07-09)*
- [x] **Snapped/erased polygons could become invalid and break `/plan`** — independent per-vertex nearest-node snapping (and occasionally `turf.difference` itself) can produce a self-intersecting ring; OSMnx then failed with "The geometry of `polygon` is invalid." Added a shared `clean_poly()` helper (shapely `buffer(0)` repair) used by every endpoint that accepts a client-supplied polygon *(resolved 2026-07-09)*
- [x] **OSM undercounted addresses (e.g. Pontefract North: 1503 vs real ~11k)** — switched primary address source to OS Open UPRN (official GB address point list, `get_uprn.py` one-time download+build); OSM addr:housenumber kept as fallback when uprn.sqlite isn't present *(resolved 2026-07-08)*
- [x] **Addresses snapped to footways** — houses counted against rear footpaths instead of their street; now snaps by addr:street name match first, and only ever to addressable road types *(resolved 2026-07-08)*
- [x] **Industrial/commercial addresses included** — now excluded by OSM landuse polygons, toggleable, default on *(resolved 2026-07-08)*
- [x] **Runt tail routes** — greedy partition left 22-address leftover routes; added adjacent-chunk merge pass in `partition()` *(resolved 2026-07-08)*
- [x] **Route sizes far too variable** — partition() now derives n_routes = round(total/target) and forces exactly that many chunks, then a diffusion-style rebalance pass moves boundary streets across multiple hops (not just direct neighbours) toward the mean; also now reports each route's convex-hull land area (km²) alongside address count *(resolved 2026-07-08)*
- [x] **Ward search box too narrow** — was sized for the old side-by-side "pan to place" box; now full sidebar width *(resolved 2026-07-08)*
- [x] **Ward outline disappeared when drawing inside it / no toggle-off** — ward boundaries always load for the visible map area (no checkbox), and the selected ward's outline persists while drawing a sub-area; a "Full ward" button restores whole-ward planning *(resolved 2026-07-08)*
- [x] **No way to exclude industrial/commercial addresses from a drawn shape** — checkbox added, on by default, filters by OSM landuse polygons *(resolved 2026-07-08)*
- [x] **No hill-aware splitting / no progress visibility** — optional steep-grade route-boundary splitting (open-elevation lookup) and a live NDJSON progress log in the sidebar during planning *(resolved 2026-07-08)*
