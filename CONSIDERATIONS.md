- **"Route size tails on isolated estates" — partial fix only.** I added a defensive guard in `_rebalance.transfer_bundle` that excludes pieces lying beyond a zero-address bridge from any transfer, so the rebalancer won't yank a whole pocket across a river/rail crossing. All existing tests still pass. But I couldn't write a clean failing-then-passing test that captures the exact real-ward failure mode: the synthetic case I built either got absorbed in greedy seeding (not in `_rebalance`) or got correctly rebalanced already. The fix is principled but unverified against a reproducer. To know if it's actually the right change, would need either (a) a real failing ward dump from the user's actual problematic ward to construct a reproducer, or (b) guidance on which of these is the right interpretation: (i) greedy should never seed a chunk across a zero-address bridge boundary at all (more invasive — touches partition() not _rebalance()), (ii) transfer_bundle should refuse bridges (what I did), or (iii) something else. Sticking with (ii) for now, but flagging it as "principled, untested against a real reproducer."
- **Sidebar list-row styling: `.routelink` vs `#wardresults div`.** `index.html:48-49` (`.routelink`, saved routes list) uses `padding: 6px 8px`, `background: #1c1f26`, `border-radius: 6px`; `index.html:36-37` (`#wardresults div`, ward search results) uses `padding: 5px 8px`, `background: #22252c`, no border-radius. Both are clickable sidebar rows for picking something. No functional reason found for the difference — needs a call on whether they should share one row style.
- **Three different reds with no shared token**: `#e15759` (residential dot / route palette), `#e11` (disconnected-fragment warning), `#c00` (split-preview chord) — three distinct reds for three different "attention" meanings in the map UI. Worth deciding whether these should consolidate to fewer tokens or are intentionally distinct per-context.
- **Two/three "primary" blues used for different roles**: `#4a9eff` (buttons, ward outline, active doc-choose state, progress bar) vs `#2563eb` (orphan/"other route" marks, take-tool highlight) vs `#1d4ed8` (POI/orientation markers). Could be a deliberate UI-chrome-vs-map-annotation split, or organic drift. Needs a human call.
- **Delta-badge colours (`updateAddrBadge`, added/removed counts) don't reuse the map's own red/green for the same concepts** — the badge uses one-off `#f77`/`#6d6` while the map marks "not to visit" black (`#111`) and "take" green as `#059669`. Consider reusing the map's established colours in the badge, or confirm the divergence is intentional.
